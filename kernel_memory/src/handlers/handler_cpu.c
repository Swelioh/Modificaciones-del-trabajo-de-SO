#include "handler_cpu.h"

void handler_cpu(t_args* args) {
    printf("Entre al handler cpu\n");

    // TO DO
    // while (1)
    // switch cases
    // -> devolver instruccion
    // -> guardar contexto
    // -> devolver contexto 
}
